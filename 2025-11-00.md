Cambio en tabla de PRODUCTOS.
	El cambio es el tipo de variable Int -> Varcahr(100)
CODE:
```
ALTER TABLE "PRODUCTOS"
ALTER COLUMN "codigo_proveedor" TYPE VARCHAR(100),
ALTER COLUMN "codigo_personal" TYPE VARCHAR(100);
```

Modificacion de TABLAS